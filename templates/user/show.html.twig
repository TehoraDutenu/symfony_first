{% extends 'base.html.twig' %}

{% block title %}Profil utilisateur
{% endblock %}

{% block body %}
	<h1>Profil utilisateur</h1>

	<div class="card">
		<h5 class="card-header">{{ user.email }}</h5>
		<div class="card-body">
			<span>
				{% for role in user.roles %}
					{% if role == 'ROLE_ADMIN' %}
						<span class="badge rounded-pill bg-success">Admin</span>
					{% elseif role == 'ROLE_USER' %}
						<span class="badge rounded-pill bg-primary">Utilisateur</span>
					{% else %}
						<span class="badge rounded-pill bg-secondary">Pas de rôle</span>
					{% endif %}

				{% endfor %}
			</span>
		</div>
		<a class="btn btn-warning" href="{{ path('app_user_edit', {'id': user.id}) }}">Modifier</a>
	</div>
</div>

<a class="btn btn-success m-2" href="{{ path('app_user_index') }}">Retour à la liste</a>


{{ include('user/_delete_form.html.twig') }}{% endblock %}{# {% extends 'base.html.twig' %}
	
	{% block title %}User{% endblock %}
	
	{% block body %}
	
	    <h1>Profil utilisateur</h1>
	
	    <div class="card my-4" style="width: 25rem;">
	        <div class="card-body d-flex flex-column">
	            <h2 class="card-title text-center">{{ user.email }}</h2>
	            <div class="d-flex my-4 justify-content-center">
	                {% for role in user.roles %}
	                    {% if role == 'ROLE_ADMIN' %}
	                        <span class="badge rounded-pill bg-success m-2 fs-5">admin</span>
	                    {% elseif role == 'ROLE_USER' %}
	                        <span class="badge rounded-pill bg-primary m-2 fs-5">utilisateur</span>
	                    {% else %}
	                        <span class="badge rounded-pill bg-grey m-2 fs-5">pas de role</span>
	                    {% endif %}
	                {% endfor %}
	            </div>
	            <a class="btn btn-warning m-2" href="{{ path('app_user_edit', {'id': user.id}) }}">Modifier</a>
	        </div>
	    </div>
	
	    <div class="d-flex">
	        <a class="btn btn-primary m-2" href="{{ path('app_user_index') }}">Retour à la liste</a>
	        {{ include('user/_delete_form.html.twig') }}
	    </div>
	
	{% endblock %}
	
	 
	
	Dispose d’un menu contextuel #}
